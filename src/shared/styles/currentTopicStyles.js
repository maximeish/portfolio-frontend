import styled from "@emotion/styled"

export const TopicText = styled.p`
  color: #6c63ff;
  font-weight: 600;
  font-style: normal;
  font-size: 16px;
  line-height: 24px;
  letter-spacing: 0.05em;
`
